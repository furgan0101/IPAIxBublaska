{
  "name": "vostbw-osint-dashboard",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "setup": "cd backend && pip install -r requirements.txt && cd ../frontend && npm install",
    "dev": "concurrently -n api,web -c magenta,cyan \"npm run dev:api\" \"npm run dev:web\"",
    "dev:api": "cd backend && python3 -m uvicorn main:app --reload --port 8000",
    "dev:web": "cd frontend && npm run dev",
    "test:api": "cd backend && python3 -m pytest -q"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
