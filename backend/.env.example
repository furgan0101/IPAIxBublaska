# Drop-in point for live AI verification (see logic/verification.py).
# Leaving these empty keeps the pipeline in mock mode — no external calls,
# no credits burned (hackathon guardrail).
ANTHROPIC_API_KEY=
GEOCODING_API_KEY=
